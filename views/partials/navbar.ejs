<nav class="navbar navbar-expand-lg fixed-top navbar-dark" style="background-color: #1565c0;">
  <div class="container">
    <a class="navbar-brand" href="/products">
      <i class="fas fa-shopping-basket" style="margin-right: 10px; font-size: 1.7rem; color: #e3f2fd;"></i>
      <span style="color: #ffffff;">HealthCare-Hub</span>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarTogglerDemo01"
      aria-controls="navbarTogglerDemo01"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <ul class="navbar-nav me-auto mb-lg-0">
        <li class="nav-item">
          <a class="nav-link text-white" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="/products">Products</a>
        </li>
        <% if(currentUser && currentUser.role === 'admin') { %>
        <li class="nav-item">
          <a class="nav-link text-white" href="/product/new">New</a>
        </li>
        <% } %>
      </ul>
      <div class="d-flex align-items-center">
        <% if (!currentUser) { %>
        <a href="/signin" class="nav-link text-white mx-2">Login</a>
        <% } else { %>
          <% if(currentUser.role === 'user') { %>
          <a href="/user/orders" class="nav-link text-white text-capitalize" style="text-decoration: underline;">
            Hello,<%= currentUser.username %>
          </a>
          <% } else { %>
          <a href="/seller/profile" class="nav-link text-white text-capitalize" style="text-decoration: underline;">
            Hello,<nav class="navbar navbar-expand-lg fixed-top navbar-dark" style="background-color: #1565c0;">
              <div class="container">
                <a class="navbar-brand" href="/products">
                  <i class="fas fa-shopping-basket" style="margin-right: 10px; font-size: 1.7rem; color: #e3f2fd;"></i>
                  <span style="color: #ffffff;">HealthCare-Hub</span>
                </a>
                <button
                  class="navbar-toggler"
                  type="button"
                  data-bs-toggle="collapse"
                  data-bs-target="#navbarTogglerDemo01"
                  aria-controls="navbarTogglerDemo01"
                  aria-expanded="false"
                  aria-label="Toggle navigation"
                >
                  <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
                  <ul class="navbar-nav me-auto mb-lg-0">
                    <li class="nav-item">
                      <a class="nav-link text-white" aria-current="page" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                      <a class="nav-link text-white" href="/products">Products</a>
                    </li>
                    <% if(currentUser && currentUser.role === 'admin') { %>
                    <li class="nav-item">
                      <a class="nav-link text-white" href="/product/new">New</a>
                    </li>
                    <% } %>
                  </ul>
                  <div class="d-flex align-items-center">
                    <% if (!currentUser) { %>
                    <a href="/signin" class="nav-link text-white mx-2">Login</a>
                    <% } else { %>
                      <% if(currentUser.role === 'user') { %>
                      <a href="/user/orders" class="nav-link text-white text-capitalize" style="text-decoration: underline;">
                        Hello, <%= currentUser.username %>&nbsp;
                      </a>
                      <% } else { %>
                      <a href="/seller/profile" class="nav-link text-white text-capitalize" style="text-decoration: underline;">
                        Hello, <%= currentUser.username %>"&nbsp;"
                      </a>
                      <% } %>
                      <p>&nbsp;</p>    
                      <a href="/user/cart" class="nav-link">&nbsp;
                        <i class="fas fa-shopping-cart" style="color: #e8f5e9;"></i>
                        <sup><span class="badge bg-danger"><%= currentUser.cart.length %></span></sup>
                      </a>
                      <a href="/signout" class="nav-link text-white mx-2">Logout</a>
                    <% } %>
                  </div>
                </div>
              </div>
            </nav>
            <%= currentUser.username %>
          </a>
          <% } %>          
          <a href="/user/cart" class="nav-link">
            <i class="fas fa-shopping-cart" style="color: #e8f5e9;"></i>
            <sup><span class="badge bg-danger"><%= currentUser.cart.length %></span></sup>
          </a>
          <a href="/signout" class="nav-link text-white mx-2">Logout</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
